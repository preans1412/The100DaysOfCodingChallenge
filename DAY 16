389. Find the Difference
Link for the Question - https://lnkd.in/d7muYdwZ



class Solution {
    public char findTheDifference(String s, String t) {
        int i=0;
        int len=s.length();
       int sum=0;
       for( i=0;i<len;i++)
       {
           sum+= t.charAt(i)- s.charAt(i);
       
       }
       sum+= t.charAt(i);
       return (char)sum;
    }
}
